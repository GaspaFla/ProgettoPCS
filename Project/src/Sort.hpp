
#include "GeometryLibrary.hpp"


using namespace std;

namespace DFN{
void Merge(vector<Traccia>& v,vector<unsigned int>& vId,const unsigned int& sx,const unsigned int& cx,const unsigned int& dx);
void MergeSort(vector<Traccia>& v,vector<unsigned int>& vId, const unsigned int& sx, const unsigned int& dx);
void MergeSort(vector<Traccia>& v,vector<unsigned int>& vId);
}
